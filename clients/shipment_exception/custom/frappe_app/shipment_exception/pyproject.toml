[project]
name = "shipment_exception"
version = "0.1.0"
description = "Shipment exception project-specific extension"
requires-python = ">=3.10"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.package-data]
"*" = ["*.json", "*.txt"]
