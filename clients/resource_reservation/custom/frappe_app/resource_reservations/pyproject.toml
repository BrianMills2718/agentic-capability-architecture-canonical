[project]
name = "resource_reservations"
version = "0.1.0"
description = "Project-local shared resource reservation workflow"
requires-python = ">=3.10"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.package-data]
"*" = ["*.json", "*.txt", "*.js"]
