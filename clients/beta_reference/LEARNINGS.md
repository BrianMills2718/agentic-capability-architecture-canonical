# Reuse assessment

The approval threshold is intentionally local because it is a Beta-specific
business rule. The scheduled reminder flow is a reuse candidate: a later
project with materially different reminder timing or recipient policy should
exercise this interface before it is promoted into a shared scheduling
capability.
