# Reuse assessment

The >90-minute approval threshold remains local to Beta because it is a
project-specific policy. The reminder extension demonstrates a reusable
integration constraint: automatic email side effects need both an explicit
scheduler trigger and durable idempotency state. Do not promote the policy or
the delivery DocType into a shared capability until a second materially
different project needs the same behavior.
