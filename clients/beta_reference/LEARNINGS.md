# Reuse assessment

Beta confirms that project-specific approval thresholds and reminder timing
belong in a client extension while the shared approvals resolver and email
transport remain reusable. The durable reminder marker is a plausible reuse
candidate, but it should not be promoted until a second materially different
project needs the same delivery semantics.
