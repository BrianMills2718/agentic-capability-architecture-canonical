[project]
name = "beta_rules"
version = "0.1.0"
description = "BETA-specific appointment rules and reminders"
requires-python = ">=3.10"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.package-data]
"*" = ["*.json", "*.txt"]
