# Compatibility Tests

Tests in this directory protect composition across proven/core capabilities and known consumers.

Add tests when:

- a shared interface changes,
- dependencies change,
- configuration semantics change,
- rule priority/conflict behavior changes,
- a third materially different use exercises a reusable capability.
