[project]
name = "compiled_appointment_proof"
version = "0.1.0"
description = "Proof-only Frappe adapter for the compiled appointment requirement"
requires-python = ">=3.10"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.package-data]
"*" = ["*.json", "*.txt"]
