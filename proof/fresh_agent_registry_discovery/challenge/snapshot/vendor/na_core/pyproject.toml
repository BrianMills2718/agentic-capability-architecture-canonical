[project]
name = "na_core"
version = "0.1.0"
description = "Foundational semantic primitives for composable Frappe projects"
requires-python = ">=3.10"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.package-data]
"*" = ["*.json", "*.txt"]
